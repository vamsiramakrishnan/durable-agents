# Pinned versions for the docs build. Used by CI + `pip install -r docs/requirements.txt`
# for local previews (`mkdocs serve`).
mkdocs==1.6.1
mkdocs-material==9.5.49
mkdocstrings[python]==0.27.0
mkdocs-autorefs==1.4.4
# pymdown-extensions <10.21 + Pygments >=2.20 crashes with `AttributeError:
# 'NoneType' object has no attribute 'replace'` when rendering code blocks
# without a `title=`. The CLI now pulls in Textual (for `tape inspect`),
# which transitively requires Pygments >=2.19.2 — so the old "pin Pygments
# down" workaround is no longer viable. Use the pymdown-extensions release
# that handles the new HtmlFormatter contract instead.
pymdown-extensions==10.21.3

# The CLI generator needs the CLI's runtime deps (Typer + pydantic + jinja2 + rich).
# typer <0.16 is incompatible with click >=8.2 (Parameter.make_metavar
# signature changed); 0.25.1 is current and works against click 8.4.
typer==0.25.1
rich==13.9.4
pydantic==2.9.2
pydantic-settings==2.6.1
PyYAML==6.0.2
Jinja2==3.1.4
