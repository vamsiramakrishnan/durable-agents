# SDK Reference

Identical surface in every language, with language-idiomatic shape.

| Concern                           | [Python](python/index.md) | [Go](go/index.md) | [TypeScript](typescript/index.md) | [Java](java/index.md) |
|---|---|---|---|---|
| Wire the runtime in one call      | `tape.adk.durable_app(...)`      | `tape.NewDurableApp(ctx, cfg)`     | `durableApp({...})`                | `DurableApp.wire(new Config().…)`     |
| Outbox tool for non-idempotent    | `@tape.outbox_tool(...)`         | `tape.NewOutboxTool(opts)`         | `outboxTool(fn, opts)`             | `OutboxTool.builder(name, conn).…`    |
| Capability connector registry     | `tape.connectors.register(...)`  | `connectors.Default`               | `CONNECTORS`                       | `ConnectorRegistry.DEFAULT`           |
| Structured logs + OTel spans      | `tape.obs.log_json` / `span`     | `tape.LogJSON` / `tape.Span`       | `logJson` / `span` / `setSpanHook` | `Obs.logJson` / `Obs.span`            |
| Tenancy + DESIGN-ONLY warn        | `tape.TenancyConfig`             | `tape.TenancyConfig`               | `tenancyFromObject` / `warnIf…`    | `Tenancy.Config`                      |

All four enforce the same `non_idempotent` safety rule **at construction time**: no `business_key`,
no `status_check`, no `compensate` ⇒ the SDK refuses to build the tool. The Rust server also
enforces the contract at `BeginEffect`-time, so even an older SDK can't slip through.

The CLI stays Python; the agent process can be in any of the four languages.

Pick your language:

<div class="grid cards" markdown>

-   :material-language-python: **[Python](python/index.md)**
    The reference SDK + ADK adapter. API generated from docstrings via `mkdocstrings`.

-   :simple-go: **[Go](go/index.md)**
    Generated from the package's godoc via `gomarkdoc`.

-   :material-language-typescript: **[TypeScript](typescript/index.md)**
    Generated from TSDoc via `typedoc --plugin typedoc-plugin-markdown`.

-   :fontawesome-brands-java: **[Java](java/index.md)**
    Generated by `mvn javadoc:javadoc`; embedded as a sibling site at `reference/java/javadoc/`.

</div>
