# tape — local dev
TAPE_URL=tape://localhost:7878

# tape — GCP (production)
GOOGLE_CLOUD_PROJECT=your-project
GOOGLE_CLOUD_LOCATION={{ region }}
# TAPE_STORE_URL is read from Secret Manager in production.
# For local Postgres dev only:
# TAPE_STORE=postgres://tape:tape@localhost:5432/tape
