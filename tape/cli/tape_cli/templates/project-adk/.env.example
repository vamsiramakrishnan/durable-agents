# Copy to .env and adjust. `tape dev` loads this.

# The SQLAlchemy store. SQLite for dev; postgresql+asyncpg://… for prod.
TAPE_ADK_DB_URL=sqlite+aiosqlite:///./.tape/dev.db

# Your model API key (ADK reads this — see the google-adk docs).
# GOOGLE_API_KEY=
